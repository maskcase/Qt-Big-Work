## 七、修改记录

> 本节记录所有对代码的变更，按时间倒序排列。每次修改必须包含：**修改原因**、**修改的文件**、**修改前后的完整代码对比**、**验证结果**。







---
---

### 【修改 #8】2026-05-31 —— 技能GIF化 + Projectile性能优化 + 主角2×2显示 + 爆炸效果 + 传送门修复

#### 修改原因
1. **I技能视觉效果升级**：从代码绘制的椭圆改为 `fly_fire_right.gif` / `fly_fire_left_down.gif` 火球，支持8方向自动旋转/翻转
2. **J技能视觉效果升级**：从绘制矩形改为 `fire_hit.gif` 火焰打击特效，持续2秒后消失
3. **J技能崩溃修复**：`QMovie::frameChanged` 槽中直接调用 `stop()` 并 `delete` 发送者，导致Qt信号处理中销毁对象而闪退
4. **I技能爆炸效果**：Projectile死亡（撞墙/敌人/出界/达到最大距离）时播放 `bomb.gif` 爆炸动画
5. **I技能卡顿优化**：连续按I会堆积大量QMovie同时解码GIF，导致帧率暴跌。彻底去掉QMovie，改用全局预加载帧缓存 + 手动切帧
6. **爆炸动画太慢**：`bomb.gif` 共95帧原始播放1.9秒，QMovie倍速无效。改为预加载帧数组，每2帧取1帧，QTimer 8ms手动切帧，约0.38秒播完
7. **主角显示太小**：从32×32改为64×64（2×2 tile），但保持右下角32×32作为唯一碰撞判定框，避免狭窄通道卡死
8. **传送门卡墙**：64×64显示导致 `sceneBoundingRect()` 过早触发传送，落点直接卡进墙里

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `skill.h` / `skill.cpp` | Projectile去QMovie，预加载8方向帧缓存；暴露 `preloadProjectileFrames()` |
| ✏️ 修改 | `game.cpp` | J技能fire_hit特效、I技能爆炸效果、 Projectile速度调整、传送门safeTeleportTo、预加载调用 |
| ✏️ 修改 | `game.h` | 新增 `createExplosion()` 声明 |
| ✏️ 修改 | `player.h` / `player.cpp` | 显示缩放64×64，新增 `hitboxRect()` |
| ✏️ 修改 | `tilemap.h` / `tilemap.cpp` | 新增 `collidesWithWall(const QRectF&)` 重载 |
| ✏️ 修改 | `resources.qrc` | 新增 `bomb.gif`、`fire_hit.gif`、`fly_fire_right.gif`、`fly_fire_left_down.gif` |
| ➕ 新增 | `images/bomb.gif` | 爆炸动画（720×720，预加载后缩放到96×96） |
| ➕ 新增 | `images/fire_hit.gif` | 普攻火焰打击特效 |
| ➕ 新增 | `images/fly_fire_right.gif` | 水平方向火球（用于右/左/上/下） |
| ➕ 新增 | `images/fly_fire_left_down.gif` | 斜向火球（用于4个对角线方向） |

---

#### 8.1 skill.h / skill.cpp —— Projectile彻底重构，去掉QMovie

**skill.h 变更**：
- 删除 `QMovie *movie` 成员、`pixmapTransform` 成员、`onFrameChanged(int)` slot
- 新增 `dirIndex`、`frameIndex`、`frameTick` 成员
- 新增全局函数 `void preloadProjectileFrames();`

**skill.cpp 核心逻辑**：
```cpp
// 匿名命名空间中全局预加载
namespace {
    QVector<QVector<QPixmap>> g_projectileFrames;
    bool g_framesLoaded = false;

    void doLoadProjectileFrames() {
        // 用 QImageReader 提取 fly_fire_right.gif 和 fly_fire_left_down.gif 的所有帧
        // 为8个方向各生成变换后的帧序列，存入 g_projectileFrames[8]
    }
}

void preloadProjectileFrames() { doLoadProjectileFrames(); }

// Projectile 构造函数中不再创建 QMovie，直接从缓存取帧
// update() 中每 2 个游戏帧手动切一帧：
frameTick++;
if (frameTick >= 2) {
    frameTick = 0;
    const auto &frames = framesForDir(dirIndex);
    frameIndex = (frameIndex + 1) % frames.size();
    setPixmap(frames[frameIndex]);
}
```

---

#### 8.2 game.cpp —— J技能特效、爆炸效果、传送门修复

**J技能（skillNormalAttack）fire_hit 特效**：
```cpp
QMovie *hitMovie = new QMovie(":/images/fire_hit.gif");
connect(hitMovie, &QMovie::frameChanged, [hitItem, hitMovie](int) {
    QPixmap frame = hitMovie->currentPixmap();
    frame = frame.scaled(96, 96, Qt::KeepAspectRatio, Qt::SmoothTransformation);
    hitItem->setPixmap(frame);
});
// 延迟 stop + 延迟 delete，避免在信号处理中销毁 QMovie
connect(hitMovie, &QMovie::stateChanged, ... QTimer::singleShot(0, ...) ...);
QTimer::singleShot(2000, ... hitMovie->stop() ...); // 2秒后消失
```

**爆炸效果（createExplosion）**：
```cpp
// 预加载 bomb.gif 帧，每2帧取1帧，缩放到96×96
namespace {
    QVector<QPixmap> g_bombFrames;
    void loadBombFrames() { ... QImageReader ... scaled(96,96) ... }
}

void Game::createExplosion(QPointF centerPos) {
    QGraphicsPixmapItem *item = new QGraphicsPixmapItem();
    item->setPos(centerPos.x() - 48, centerPos.y() - 48);
    item->setPixmap(g_bombFrames[0]);

    QTimer *timer = new QTimer(this);
    int *frameIdx = new int(0);
    connect(timer, &QTimer::timeout, [timer, item, frameIdx]() {
        (*frameIdx)++;
        if (*frameIdx >= g_bombFrames.size()) {
            timer->stop(); timer->deleteLater();
            delete item; delete frameIdx;
            return;
        }
        item->setPixmap(g_bombFrames[*frameIdx]);
    });
    timer->start(8); // 8ms一帧，约0.38秒播完
}
```

**传送门修复**：
```cpp
// 检测从 sceneBoundingRect() 改为 hitboxRect()
QRectF playerRect = player->hitboxRect();

// 传送落点自动防卡墙
auto safeTeleportTo = [&](const QPointF &target) {
    QPointF basePos = target - QPointF(32, 32);
    player->setPos(basePos);
    if (tileMap->collidesWithWall(player->hitboxRect())) {
        // 尝试8个方向各偏移32像素，直到不卡墙
        ...
    }
};
```

---

#### 8.3 player.h / player.cpp —— 2×2显示 + 右下角碰撞框

```cpp
// player.h
QRectF hitboxRect() const { return QRectF(x() + 32, y() + 32, 32, 32); }

// player.cpp 显示缩放改为 64×64
qreal sx = 64.0 / origSize.width();
qreal sy = 64.0 / origSize.height();

// move() 中碰撞检测改用 hitboxRect()
if (tileMap->collidesWithWall(hitboxRect()))
    setPos(x() - dx, y());
```

---

#### 8.4 tilemap.h / tilemap.cpp —— QRectF碰撞重载

```cpp
// tilemap.h
bool collidesWithWall(QGraphicsItem *item) const;
bool collidesWithWall(const QRectF &rect) const;

// tilemap.cpp
bool TileMap::collidesWithWall(const QRectF &rect) const {
    for (Tile *wall : walls) {
        if (wall->sceneBoundingRect().intersects(rect))
            return true;
    }
    return false;
}
```

---


### 【修改 #7】2026-05-30 —— 血量蓝量系统 + 攻击扣血生效 + 地图怪物全部活化

#### 修改原因
用户反馈三个严重问题：
1. **玩家和怪物缺乏血量蓝量系统**：玩家需要 HP/MP，每秒恢复 1 点；怪物需要有血量条显示
2. **攻击扣血不生效**：玩家技能和普攻只输出 qDebug 没有实际扣血；怪物炮弹只输出 qDebug 没有实际扣玩家血
3. **地图中大量怪物静止不动**：chamber1.tmj 中有 `minion_image` 图层（88 个非空瓦片），之前被当作静态 Tile 处理，不会移动和攻击

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `player.h` | 添加 HP/MP 成员和方法声明 |
| ✏️ 修改 | `player.cpp` | 实现 takeDamage / consumeMp / recoverHpMp |
| ✏️ 修改 | `enemy.h` | 添加 maxHp、血条前景/背景成员 |
| ✏️ 修改 | `enemy.cpp` | 实现血条创建、跟随、受伤更新、死亡隐藏 |
| ✏️ 修改 | `game.h` | 添加 regenCounter 恢复计时器 |
| ✏️ 修改 | `game.cpp` | 修复所有攻击扣血、技能耗蓝、minion_image 转 Enemy、HP/MP 恢复 |

---

#### 7.1 player.h / player.cpp —— 玩家血量蓝量系统

**player.h 新增**：

```cpp
    int getHp() const { return hp; }
    int getMaxHp() const { return maxHp; }
    int getMp() const { return mp; }
    int getMaxMp() const { return maxMp; }

    void takeDamage(int dmg);
    bool consumeMp(int cost);
    void recoverHpMp(int hpAmount, int mpAmount);

private:
    int hp = 100;
    int maxHp = 100;
    int mp = 100;
    int maxMp = 100;
```

**player.cpp 实现**：

```cpp
void Player::takeDamage(int dmg)
{
    hp -= dmg;
    if (hp < 0) hp = 0;
    qDebug() << "Player took damage:" << dmg << "HP:" << hp << "/" << maxHp;
}

bool Player::consumeMp(int cost)
{
    if (mp < cost) {
        qDebug() << "Not enough MP! Need" << cost << "have" << mp;
        return false;
    }
    mp -= cost;
    qDebug() << "Consumed MP:" << cost << "MP left:" << mp << "/" << maxMp;
    return true;
}

void Player::recoverHpMp(int hpAmount, int mpAmount)
{
    hp += hpAmount;
    if (hp > maxHp) hp = maxHp;
    mp += mpAmount;
    if (mp > maxMp) mp = maxMp;
}
```

---

#### 7.2 enemy.h / enemy.cpp —— 怪物血条显示

**enemy.h 新增**：

```cpp
    int maxHp;            // 最大生命值
    QGraphicsRectItem *hpBarBg = nullptr;  // 血条背景（灰色）
    QGraphicsRectItem *hpBarFg = nullptr;  // 血条前景（红色）

    void updateHpBar();   // 更新血条宽度
```

**enemy.cpp 血条创建**（构造函数中）：

```cpp
    // 创建血条（位于怪物头顶）
    hpBarBg = new QGraphicsRectItem(-16, -10, 32, 4);
    hpBarBg->setBrush(QBrush(Qt::darkGray));
    hpBarBg->setPen(Qt::NoPen);
    scene->addItem(hpBarBg);

    hpBarFg = new QGraphicsRectItem(-16, -10, 32, 4);
    hpBarFg->setBrush(QBrush(Qt::red));
    hpBarFg->setPen(Qt::NoPen);
    scene->addItem(hpBarFg);
```

**enemy.cpp 血条跟随**（update 中）：

```cpp
    // 更新血条位置（跟随怪物）
    if (hpBarBg) hpBarBg->setPos(pos());
    if (hpBarFg) hpBarFg->setPos(pos());
```

**enemy.cpp 血条更新**（takeDamage 中）：

```cpp
void Enemy::takeDamage(int dmg)
{
    if (dead) return;
    hp -= dmg;
    qDebug() << "Enemy took damage:" << dmg << "HP left:" << hp << "/" << maxHp;

    // 更新血条宽度比例
    if (hpBarFg && maxHp > 0) {
        qreal ratio = static_cast<qreal>(hp) / maxHp;
        qreal width = 32.0 * ratio;
        hpBarFg->setRect(-16, -10, width, 4);
    }

    if (hp <= 0) {
        hp = 0;
        dead = true;
        // 死亡时隐藏血条
        if (hpBarBg) hpBarBg->hide();
        if (hpBarFg) hpBarFg->hide();
    }
}
```

> **血条设计**：宽 32px、高 4px，位于怪物头顶上方 10 像素处。背景灰色，前景红色。受伤时前景矩形宽度按比例缩小。

---

#### 7.3 game.cpp —— 攻击扣血生效 + 技能耗蓝 + 怪物活化

**修改点 1：minion_image 图层转换为 Enemy（解决静止怪物问题）**

```cpp
// targetLayers 中加入 "minion_image"
QStringList targetLayers = { "door", "chest", "boss_image", "portal_image", "minion_image" };

// 在图层遍历循环中：
if (layerName == "minion_image") {
    Enemy *enemy = new Enemy(tileMap, scene, QPointF(x * tileWidth, y * tileWidth), this);
    enemies.append(enemy);
    hittableItems.append(enemy);
    continue; // 跳过 Tile 创建
}
```

> **效果**：chamber1.tmj 中的 88 个 minion 瓦片全部被转换为会动的 Enemy 对象，随机游走并发射紫色炮弹。

**修改点 2：技能添加 MP 消耗检查**

| 技能 | 按键 | MP 消耗 | 不足时的行为 |
|------|------|---------|-------------|
| 粒子爆发 | I | 10 | `consumeMp` 返回 false，技能不释放 |
| 闪现刀浪 | K | 15 | `consumeMp` 返回 false，技能不释放 |
| 盾牌 | L | 5 | `consumeMp` 返回 false，盾牌不开启 |
| 普攻 | J | 0 | 不耗蓝 |

**修改点 3：玩家攻击实际扣血（替换所有 qDebug 预留）**

```cpp
// skillNormalAttack 中：
Enemy *enemy = dynamic_cast<Enemy*>(hittable);
if (enemy) {
    enemy->takeDamage(15);
}

// updateProjectiles 中：
Enemy *enemy = dynamic_cast<Enemy*>(hittable);
if (enemy) {
    enemy->takeDamage(p->getDamage());
}

// updateBladeWaves 中：
Enemy *enemy = dynamic_cast<Enemy*>(hittable);
if (enemy) {
    enemy->takeDamage(bw->getDamage());
}
```

**修改点 4：怪物攻击实际扣玩家血**

```cpp
// updateEnemyProjectiles 中：
if (alive && player && ep->collidesWithItem(player)) {
    if (shieldActive) {
        qDebug() << "Enemy projectile blocked by shield!";
    } else {
        player->takeDamage(ep->getDamage());  // ← 实际扣血
    }
    alive = false;
}
```

**修改点 5：每秒恢复 1 HP 和 1 MP**

```cpp
// updateGame 中：
regenCounter++;
if (regenCounter >= 60) {  // 60 帧 ≈ 1 秒
    regenCounter = 0;
    if (player) {
        player->recoverHpMp(1, 1);
    }
}
```

---

#### 7.4 编译与验证结果

```bash
cd /Users/explorer/Desktop/code/Qt-Gaming-main
make clean
qmake Qt-Gaming.pro
make -j$(sysctl -n hw.ncpu)
```

**编译结果**：✅ **成功，0 错误，0 警告**

**运行验证**：
```bash
open Qt-Gaming.app
```

| 测试项 | 结果 |
|--------|------|
| school_map 敌人 | 3 个手动创建的 Enemy 正常游走攻击 ✅ |
| chamber1 敌人 | 88 个 minion 瓦片全部转换为会动的 Enemy ✅ |
| 怪物血条 | 每个 Enemy 头顶显示红色血条，受伤时缩短 ✅ |
| 怪物死亡 | HP 归零后停止移动，血条隐藏，从场景移除 ✅ |
| J 普攻扣血 | 九宫格范围内 Enemy 实际扣 15 HP ✅ |
| I 流星扣血 | 流星击中 Enemy 实际扣 25 HP ✅ |
| K 刀浪扣血 | 刀浪击中 Enemy 实际扣 40 HP ✅ |
| 紫色炮弹扣玩家血 | 炮弹击中玩家时 `player->takeDamage(10)` ✅ |
| 盾牌阻挡 | 按住 L 时炮弹被阻挡，玩家不受伤害 ✅ |
| MP 消耗 | I(10) / K(15) / L(5) 均正确扣除 MP ✅ |
| MP 不足 | MP < 消耗时技能无法释放 ✅ |
| 每秒恢复 | 每 60 帧恢复 1 HP + 1 MP ✅ |
| 地图切换 | 传送后旧敌人/炮弹清理，新地图敌人正确生成 ✅ |

---

#### 7.5 对后续开发的影响

| 影响点 | 说明 |
|--------|------|
| **玩家死亡处理** | 当前 `Player::takeDamage()` 只扣血不处理死亡。后续 HP <= 0 时可触发死亡动画、回到出生点、游戏结束界面 |
| **Boss 血量** | 当前 Boss 仍是静态 Tile，无血量系统。如需 Boss 战，可将 Boss Tile 替换为 Enemy 子类或独立 Boss 类 |
| **伤害数字飘字** | 当前只有 qDebug 输出伤害。后续可在场景中创建临时 QLabel/QGraphicsTextItem 显示飘字 |
| **技能冷却 + MP 消耗 UI** | 建议在画面上方添加 HUD：玩家 HP/MP 条、技能图标冷却遮罩 |
| **敌人数量优化** | chamber1 有 88 个 Enemy，密集时可能较多炮弹。如需优化，可减少 minion_image 图层密度，或限制同屏敌人最大数量 |

---

> **文档结束**。如有新增功能或代码变更，请及时更新此文档，保持与代码同步。


---

### 【修改 #8】2026-05-30 —— 左上角 HUD 血蓝条

#### 修改原因
玩家有 HP/MP 数值但没有可视化显示，战斗中无法直观判断血量和蓝量。需要在屏幕左上角添加固定的 HUD 条。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `game.h` | 添加 HUD 成员指针和 createHud/updateHud 方法 |
| ✏️ 修改 | `game.cpp` | 实现 HUD 创建、每帧更新、清理逻辑 |

**game.h 新增**：
```cpp
    QGraphicsRectItem *hudHpBg = nullptr;
    QGraphicsRectItem *hudHpFg = nullptr;
    QGraphicsRectItem *hudMpBg = nullptr;
    QGraphicsRectItem *hudMpFg = nullptr;
    QGraphicsSimpleTextItem *hudText = nullptr;

    void createHud();
    void updateHud();
```

**game.cpp createHud()**：
```cpp
    hudHpBg = new QGraphicsRectItem(0, 0, 120, 14);
    hudHpBg->setBrush(QBrush(QColor(60, 60, 60, 200)));
    hudHpBg->setPen(QPen(Qt::black, 1));
    scene->addItem(hudHpBg);

    hudHpFg = new QGraphicsRectItem(0, 0, 120, 14);
    hudHpFg->setBrush(QBrush(QColor(220, 60, 60, 230)));
    hudHpFg->setPen(Qt::NoPen);
    scene->addItem(hudHpFg);

    hudMpBg = new QGraphicsRectItem(0, 0, 120, 14);
    hudMpBg->setBrush(QBrush(QColor(60, 60, 60, 200)));
    hudMpBg->setPen(QPen(Qt::black, 1));
    scene->addItem(hudMpBg);

    hudMpFg = new QGraphicsRectItem(0, 0, 120, 14);
    hudMpFg->setBrush(QBrush(QColor(60, 120, 220, 230)));
    hudMpFg->setPen(Qt::NoPen);
    scene->addItem(hudMpFg);

    hudText = new QGraphicsSimpleTextItem();
    hudText->setBrush(QBrush(Qt::white));
    QFont font = hudText->font();
    font.setPointSize(10);
    font.setBold(true);
    hudText->setFont(font);
    scene->addItem(hudText);
```

**game.cpp updateHud()**（每帧调用）：
```cpp
    QPointF hudPos = mapToScene(10, 10);
    qreal hpRatio = static_cast<qreal>(player->getHp()) / player->getMaxHp();
    hudHpFg->setRect(hudPos.x(), hudPos.y(), 120 * hpRatio, 14);
    hudHpBg->setRect(hudPos.x(), hudPos.y(), 120, 14);

    qreal mpRatio = static_cast<qreal>(player->getMp()) / player->getMaxMp();
    hudMpFg->setRect(hudPos.x(), hudPos.y() + 18, 120 * mpRatio, 14);
    hudMpBg->setRect(hudPos.x(), hudPos.y() + 18, 120, 14);

    hudText->setText(QString("HP:%1/%2  MP:%3/%4")
                     .arg(player->getHp()).arg(player->getMaxHp())
                     .arg(player->getMp()).arg(player->getMaxMp()));
    hudText->setPos(hudPos.x() + 2, hudPos.y() + 34);
```

> HUD 会随视野缩放一起缩放，因为放在 QGraphicsScene 中。

---

### 【修改 #9】2026-05-30 —— 随机 floor/wall 纹理 + 固定大小缩放

#### 修改原因
地图地板和墙壁纹理单一，希望每次进入地图时 floor 和 wall 随机选择变体图片。同时新图片原始分辨率与 32×32 不同，需要固定显示大小。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `resources.qrc` | 注册 floor_dark/light、wall_1/2/3.png |
| ✏️ 修改 | `tile.h` | 构造函数添加 `fixedSize` 参数 |
| ✏️ 修改 | `tile.cpp` | `QPixmap::scaled()` 固定缩放到 tileSize |
| ✏️ 修改 | `tilemap.cpp` | 创建 Tile 时随机替换 floor/wall 图片路径 |

**resources.qrc 新增**：
```xml
<file>images/floor_dark.png</file>
<file>images/floor_light.png</file>
<file>images/wall_1.png</file>
<file>images/wall_2.png</file>
<file>images/wall_3.png</file>
```

**tilemap.cpp 随机替换逻辑**：
```cpp
    if (imagePath.endsWith("floor.png")) {
        finalPath = (QRandomGenerator::global()->bounded(2) == 0)
                    ? ":/images/floor_dark.png" : ":/images/floor_light.png";
        fixedSize = QSize(tileSize, tileSize);
    } else if (imagePath.endsWith("wall.png")) {
        int r = QRandomGenerator::global()->bounded(3);
        finalPath = QString(":/images/wall_%1.png").arg(r + 1);
        fixedSize = QSize(tileSize, tileSize);
    }

    Tile *tile = new Tile(finalPath, x * tileSize, y * tileSize, fixedSize);
```

> 此时仍使用 `QPixmap::scaled()` 提前缩小，后续修改 #12 改为保留原始分辨率。

---

### 【修改 #10】2026-05-30 —— 视野缩放（+/- 键）

#### 修改原因
玩家希望可以通过 +/- 键调整视野远近：+ 键靠近角色（视野变小，角色变大），- 键远离角色（视野变大）。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `game.h` | 添加 zoomLevel、常量、applyZoom() |
| ✏️ 修改 | `game.cpp` | keyPressEvent 处理 +/-，applyZoom 实现 |

**game.h 新增**：
```cpp
    qreal zoomLevel = 1.0;
    static constexpr qreal ZOOM_STEP = 1.15;
    static constexpr qreal MIN_ZOOM = 0.3;
    static constexpr qreal MAX_ZOOM = 4.0;
    void applyZoom();
```

**game.cpp keyPressEvent**：
```cpp
    case Qt::Key_Plus:
    case Qt::Key_Equal:
        zoomLevel *= ZOOM_STEP;
        if (zoomLevel > MAX_ZOOM) zoomLevel = MAX_ZOOM;
        applyZoom();
        break;
    case Qt::Key_Minus:
        zoomLevel /= ZOOM_STEP;
        if (zoomLevel < MIN_ZOOM) zoomLevel = MIN_ZOOM;
        applyZoom();
        break;
```

**game.cpp applyZoom()**：
```cpp
void Game::applyZoom()
{
    resetTransform();
    scale(zoomLevel, zoomLevel);
    if (player) {
        centerOn(player);
    }
}
```

> 切地图时 `zoomLevel` 重置为 1.0。碰撞检测不受影响（基于场景坐标）。

---

### 【修改 #11】2026-05-30 —— SmoothTransformation 平滑渲染

#### 修改原因
放大视野后，32×32 的瓦片被拉伸会出现明显锯齿。需要让 Qt 使用双线性插值平滑渲染。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `tile.cpp` | `setTransformationMode(Qt::SmoothTransformation)` |
| ✏️ 修改 | `player.cpp` | 同上 |
| ✏️ 修改 | `enemy.cpp` | 同上 |

---

### 【修改 #12】2026-05-30 —— 保留原始分辨率 + 修复技能中心偏移

#### 修改原因
放大时图片失真：高分辨率素材（player.gif 246×240、wall_2.png 372×346）被提前缩放到 32×32，放大只能拉伸低分辨率图片。

#### 方案
保留原始分辨率，用 `QGraphicsItem::setTransform(QTransform::fromScale())` 缩放到目标显示大小。放大时清晰度显著提升。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `tile.cpp` | 去掉 `QPixmap::scaled()`，改用 `setTransform()` |
| ✏️ 修改 | `player.cpp` | 去掉 `movie->setScaledSize(32,32)`，改用 `setTransform()` |
| ✏️ 修改 | `enemy.cpp` | 去掉直接 `setPixmap()` 后无缩放，改用 `setTransform()` |
| ✏️ 修改 | `game.cpp` | 所有 `player->pos() + boundingRect().center()` 改为 `sceneBoundingRect().center()` |

**tile.cpp 核心改动**：
```cpp
    setPixmap(pixmap);
    setTransformationMode(Qt::SmoothTransformation);
    if (fixedSize.isValid()) {
        qreal sx = static_cast<qreal>(fixedSize.width())  / pixmap.width();
        qreal sy = static_cast<qreal>(fixedSize.height()) / pixmap.height();
        setTransform(QTransform::fromScale(sx, sy));
    }
```

**player.cpp 核心改动**：
```cpp
    movie->start();
    QSize origSize = movie->currentPixmap().size();
    qreal sx = 32.0 / origSize.width();
    qreal sy = 32.0 / origSize.height();
    setTransform(QTransform::fromScale(sx, sy));
```

**game.cpp 技能中心修复（关键 bug）**：
```cpp
// 之前（错误）：boundingRect() 返回未变换的 246×240，与 pos() 不在同一坐标系
QPointF center = player->pos()
                 + QPointF(player->boundingRect().width() / 2.0,
                           player->boundingRect().height() / 2.0);

// 之后（正确）：sceneBoundingRect() 考虑变换，center() 就是真正的视觉中心
QPointF center = player->sceneBoundingRect().center();
```

> **注意**：闪现逻辑中 `player->setPos()` 仍使用左上角（`player->pos()`），轨迹计算单独使用 `oldCenter/finalCenter`，两者不能混用。

---

### 编译与验证结果

```bash
cd /Users/explorer/Desktop/code/Qt-Gaming-main
make clean && qmake Qt-Gaming.pro && make -j$(sysctl -n hw.ncpu)
```

**编译结果**：✅ **成功，0 错误，0 警告**

**运行验证**：
```bash
open Qt-Gaming.app
```

| 测试项 | 结果 |
|--------|------|
| 随机 floor 纹理 | floor_dark / floor_light 随机出现 ✅ |
| 随机 wall 纹理 | wall_1 / wall_2 / wall_3 随机出现 ✅ |
| 视野放大（+） | 角色和怪物变大，清晰度高 ✅ |
| 视野缩小（-） | 视野范围扩大 ✅ |
| 平滑渲染 | 放大后边缘锯齿明显减少 ✅ |
| I 流星技能 | 从玩家中心正确发射 ✅ |
| J 普攻 | 从玩家中心正确触发 ✅ |
| K 闪现刀浪 | 闪现位置正确，轨迹从中心画出 ✅ |
| L 盾牌 | 盾牌中心跟随玩家 ✅ |
| 碰撞检测 | 玩家、敌人、wall 碰撞正常 ✅ |
| 传送门 | 碰撞检测正常 ✅ |

---

### 对后续开发的影响

| 影响点 | 说明 |
|--------|------|
| **HUD 随缩放** | HUD 放在 QGraphicsScene 中，放大时会一起变大。如需固定大小，需改为 QWidget overlay |
| **素材分辨率差异** | floor.png 等仍是 32×32 低分辨率，放大仍会失真。建议未来统一用高分辨率素材 |
| **boundingRect 陷阱** | 任何使用 `pos() + boundingRect().center()` 的代码都会因 transform 而失效，必须用 `sceneBoundingRect().center()` |
| **队友更新地图** | 只要图层名不变（wall、minion_image 等），代码自动适配。新增图层需加入 `targetLayers` 并写处理逻辑 |

---
---

### 【修改 #13】2026-06-02 —— 怪兽GIF随机化 + 地形纹理变体 + 队友代码整合

#### 修改原因
1. 怪兽使用静态 `minion.png`，视觉效果单调
2. 队友提交了按地图区分的地形纹理变体（地板/墙壁/水/火焰/草地/门朝向）
3. 队友新增 `floor_road`、`floor_room` 等图层，旧地图无这些图层
4. 需要整合队友的 `game.cpp` 改动和新图片资源

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `enemy.h/cpp` | QMovie → 预提取帧缓存；8 种 monster GIF 随机 |
| ✏️ 修改 | `game.cpp` | 合并队友纹理变体逻辑（水/火/地板/墙/草/门朝向） |
| ✏️ 修改 | `resources.qrc` | 注册 54+ 新资源文件 |
| ➕ 新增 | `images/` | 28 个地形变体 + 8 个怪兽 GIF |
| ➕ 新增 | `maps/` | 队友新版 chamber1/lianda/Weiming_lake/school_map + 4个 .tsx |

#### 怪兽帧缓存
```cpp
// 全局预提取，所有敌人共享（零运行时解码开销）
static QVector<QVector<QPixmap>> g_monsterFrames;
void preloadMonsterFrames();  // Game 构造时调用
// Enemy: 随机 monsterType，update() 每4帧切一帧
```

#### 地形纹理变体
- **水**：普通图 `water_1/2`(30/70)，Weiming_lake `water_d_1/2/3`(60/35/5)
- **火焰**：`fireland_1/2/3` 随机
- **地板**：school_map→`floor_room_1`，chamber1→`floor_road_3`/`floor_wood_1/2` 等
- **墙**：school→`wall_w_*`，chamber1→`wall_r_*`，带末端检测
- **草**：`grass_1/2/3`(85/7/8)，**门**：自动检测邻居朝向 `door_h/c`

---

### 【修改 #14】2026-06-02 —— Z值层级重构 + 怪兽子弹位置修复

#### 修改原因
1. 地形 Z=0~4 后，敌人/子弹/宠物默认 Z=0 全被盖住
2. 怪兽子弹从远处冒出（`boundingRect()` 不匹配 `setTransform`）

#### Z 值层级（底→顶）
```
Z=-4  草地/火焰    Z=0  装饰(chest/door/portal等)
Z=-3  水          Z=1  宠物/火焰背景
Z=-2  地板/路     Z=2  玩家
Z=-1  墙壁        Z=4  巢穴  Z=5敌人  Z=6敌人子弹
                  Z=30 盾  Z=50+特效  Z=1000+HUD  Z=100000变身
```

#### 怪兽子弹修复
```cpp
// 之前：boundingRect() 返回缩放前原始尺寸 → pos+(50,50) 远超 32px 视觉范围
// 之后：
QPointF center = sceneBoundingRect().center();  // 考虑 transform 的实际位置
```

---

### 【修改 #15】2026-06-02 —— 跨地图状态保持 + 传送闪退

#### Player::restoreState()
```cpp
void Player::restoreState(int lvl, int exp, int maxExp,
                           int hp, int maxHp, int mp, int maxMp, bool enhanced);
```
loadMap 删旧 player 前保存全部状态，建新后恢复。等级/HP/MP/exp/增强形态跨地图保持。

#### 传送闪退
loadMap 清理场景前先 `transformMovie->stop()` + `delete transformItem`，防 QMovie 回调野指针。

---

### 【修改 #16】2026-06-02 —— 静止K回血 + 增强形态三向移动 + 小地图

- **K 静止**：回 30 HP + 5 个红十字上升淡出
- **vertDir**：-1 上(`back_run`) / 0 水平(`right_run`) / 1 下(`front_run`)
- **小地图**：`scene->render()` 缩放 150px，视口右上角，红点标玩家位置

---

### 【修改 #17】2026-06-02 —— 受伤定身 + 钻石 + 传送门漩涡 + 攻击 buff

- **定身**：被敌人打到冻结 0.2s，4 个紫色"-"旋转上升
- **钻石**：红(补血)/蓝(补蓝)/紫(攻击翻倍10s+头顶十字)，随机放可行走地面
- **传送门**：BFS 连通区域 → 一个 `shikongxuanwo.png` 旋转 90°/12帧
- **buff**：`getBuffedDamage(base)` → attackBuffTimer>0 时 ×2

---

### 【修改 #18】2026-06-02 —— I技能扣蓝修复 + 火焰等级 + 传送禁技能

- **I 技能**：`consumeMp` 移到方向检查之后，跑动按I不扣蓝
- **火焰背景**：1级不显示，2级 `onPlayerLevelUp` 自动创建
- **传送禁技能**：`isNearPortal()` — 玩家外扩 2 格与传送门相交时禁 I/J/K/N/H/L

---

### 【修改 #19】2026-06-02 —— 传送激光 + 变身动画修复

- **传送/出生**：10 条红白交错竖线向下闪过，0.7~1.2s
- **变身动画**：`ItemIgnoresTransformations` + 屏幕坐标居中，不受场景遮挡
- **buff 十字**：高度 60→40px

---

### 【修改 #20】2026-06-03 —— 主菜单+地图介绍+动态水+加速+管理员模式+新图层+新地图（队友代码整合）

#### 修改原因
队友在原有代码基础上进行了大规模功能扩展，并同步整合了之前所有修改（#13~#19）的成果。将旧版代码归档至 `past/` 文件夹后，直接采用队友最新代码作为主版本。

#### 队友新增系统一览

| 系统 | 详情 |
|------|------|
| **主菜单** | 开始游戏/关于/退出，纯 QGraphicsScene 实现，鼠标点击交互 |
| **游戏内菜单** | 右上角 40×40 圆角菜单按钮（三横线图标），继续/关于/退出，ESC 关闭 |
| **地图介绍弹窗** | 每个地图首次进入显示 QDialog，含标题+立绘+描述文字+跳过按钮 |
| **加速技能 O** | 5 秒速度翻倍（4.0→8.0），5 秒冷却 |
| **管理员模式** | 输入 `czz` 激活/关闭，数字键 0-9 直设等级，X 切换穿墙 |
| **动态水** | `images/water/0000.png~0039.png` 40 帧序列预加载，Weiming_lake 水面 3 tick 切一帧 |
| **梅花瓣粒子** | 随机生成花瓣飘落特效，带速度和旋转 |
| **背景叠加** | school_map 拼接 `try_background_left/right.png`，Z=-0.5 全地图覆盖 |
| **小地图** | `scene->render()` 缩放到 150px 宽，视口右上角，红点标玩家 |
| **传送门漩涡** | portal 图层不创建 Tile，改为 `shikongxuanwo.png` 每 12 帧旋转 90° |
| **传送门附近禁技能** | `isNearPortal()` — 玩家外扩 2 格与传送门 rect 相交时禁 I/J/K/N/H/L/O + 菜单按钮 |

#### 新增图层支持

| 图层名 | 用途 | 纹理变体 |
|--------|------|---------|
| `floor_onfire` | 火焰地板 | Weiming_lake: `floor_road_f_1/2`，其他: `floor_road_f_1` |
| `floor_road` | 道路地板 | Weiming_lake: `floor_road_d_1/2`，chamber1: `floor_road_3`，其他: `floor_road_1/2` |
| `floor_room` | 房间地板 | Weiming_lake: `floor_room_d_1/2`(90/10)，chamber1: `floor_wood_3/4`，其他: `floor_room_1` |
| `grass/grassland` | 草地 | Weiming_lake: `grass_d_1/2`，其他: `grass_1/2/3`(85/7/8) |
| `stair_h` | 横向楼梯 | `stair_h.png` |
| `stair_c1/c2` | 纵向楼梯 | `stair_c1.png`, `stair_c2.png` |

#### 新增地图
- `new_school_map.tmj` — 新版校史馆（主菜单"开始游戏"默认加载）
- `ending.tmj` — 结局地图

#### 受伤定身 + 钻石系统 + 紫钻 buff
- **定身**：受击 12 帧（0.2s）无法移动
- **钻石**：红(+HP)/蓝(+MP)/紫(攻击翻倍)，随机放置在可行走地面
- **紫钻 buff**：持续 600 帧（10s），玩家头顶紫色十字提示，`getBuffedDamage()` 自动 ×2
- **钻石碰撞**：玩家 + 宠物均可拾取，拾取后有十字扩散特效

#### 传送/出生激光特效
- 10 条红白竖线从上方闪过，随机延迟 0.7~1.2 秒，`spawnArrivalEffect()`

#### Spawner 调整
- 生成间隔：300→600 帧（5s→10s）
- 每次生成：2→1 只
- 场上上限：20→10 只
- 巢穴数量：3→2 个（安全放置函数防卡墙/水）

#### Player 增强形态方向精灵
- `vertDir`：-1 上(`player_enhanced_back_run.gif`) / 0 水平(`player_enhanced_right_run.gif`) / 1 下(`player_enhanced_front_run.gif`)
- `updateAnimationState(moving, right, vDir)` 三参数版本

#### 已整合的之前修改
队友代码已包含 #13~#19 全部改动：怪兽帧缓存、Z 值层级、地形纹理变体、跨地图状态保持、传送闪退修复、怪兽子弹位置修复、K 静止回血、传送禁技能等。

#### 资源文件
`resources.qrc` 共注册 150 个文件，包括：
- 40 帧动态水序列 `images/water/0000.png~0039.png`
- 12 个新增地形变体（`grass_d`, `floor_road_d/f`, `floor_room_d`, `stair_h/c1/c2`, `wall_crack/d_*`）
- 3 个钻石图片（`red/blue/purple_diamond.png`）
- 2 个背景拼接图（`try_background_left/right.png`）
- 1 个传送门漩涡（`shikongxuanwo.png`）
- 2 个新地图（`new_school_map.tmj`, `ending.tmj`）

#### 编译验证
```bash
cd /Users/explorer/Desktop/code/Qt-Gaming-main
make clean && qmake && make -j4
```
✅ **编译成功，0 错误，0 警告**

---

### 【修改 #21】2026-06-03 —— 水纹理回退静态随机 + 移除主地图背景叠加

#### 修改原因
1. **动态水帧动画被移除**：40 帧序列 `images/water/0000.png~0039.png` 每帧都需要缩放和重设 pixmap，运行时开销大。改回加载时随机选一张静态水纹理（`water_d_1/2/3` 或 `water_1/2`），视觉效果差异不大但零运行时开销。
2. **主地图背景叠加被移除**：`new_school_map`（校史馆）的 `try_background_left.png` + `try_background_right.png` 拼接背景层覆盖全地图，遮挡了部分游戏对象且影响性能。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `game.h` | 移除 `animatedWaterTiles`、`waterFrameIdx`、`waterFrameTick`、`bgOverlay` 成员变量 |
| ✏️ 修改 | `game.cpp` | 移除 `preloadWaterFrames()` 函数和调用；移除 `updateGame()` 中动态水帧切换；移除 `loadMap()` 中背景叠加创建和清理；移除析构函数中的 `bgOverlay` 清理 |

#### 具体变更

**game.h** — 移除 7 行成员变量：
```cpp
-    // ========== 动态水 ==========
-    QVector<Tile*> animatedWaterTiles;
-    int waterFrameIdx = 0;
-    int waterFrameTick = 0;
-
-    // ========== 主地图背景叠加 ==========
-    QGraphicsPixmapItem *bgOverlay = nullptr;
```

**game.cpp** — 移除以下 7 处代码：
1. `preloadWaterFrames()` 全局函数（18 行）和 `g_waterFrames`/`g_waterLoaded` 全局变量
2. 构造函数中 `preloadWaterFrames()` 调用
3. water 图层中 `animatedWaterTiles.append(waterTile)`（3 行）
4. `loadMap()` 清理中 `animatedWaterTiles.clear()` 和 `bgOverlay` delete
5. `updateGame()` 中动态水帧切换循环（15 行）
6. `loadMap()` 中背景叠加创建代码块（23 行，含 QPainter 拼接、缩放、Z=-0.5 设置）
7. 析构函数中 `bgOverlay` delete

**保留**：water 图层的随机静态纹理选择逻辑完全保留（`water_d_1/2/3` 和 `water_1/2`），水碰撞检测不受影响。

#### 编译验证
```bash
cd /Users/explorer/Desktop/code/Qt-Gaming-main
make clean && qmake && make -j4
```
✅ **编译成功，0 错误，0 警告**

---

### 【文档更新 #22】2026-06-04 —— 代码说明书全面重写

#### 修改原因
基于逐行精读全部源代码，在原 `项目代码功能说明书-最新版.md` 基础上补充了大量遗漏的功能细节。

#### 新增/补充说明的功能点（共25项）
主菜单系统、游戏内菜单、地图介绍弹窗、管理员模式、调试地图查看器(F1)、O键加速技能、钻石系统(红蓝紫)、紫钻攻击buff、受伤定身、传送/出生激光特效、梅花瓣粒子系统、小地图系统、传送门漩涡动画、K静止回血、背后火焰(2级+)、全局帧缓存体系、Z值层级体系(完整20层)、技能完整参数表(7×12)、按地图区分纹理策略表、门朝向自动检测、安全放置算法、手动绘制图层完整列表(17种)、地图文件详细列表、resources.qrc详细分类(12类150文件)、Qt-Gaming.pro配置、8种monster GIF列表、已移除功能标注(动态水/背景叠加)。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ➕ 新增 | `项目代码说明书最新新版.md` | 全面重写的代码说明书(v3.0)，基于逐行精读全部11个源文件 |
| ✏️ 修改 | `修改记录.md` | 新增本文档更新记录 |

#### 阅读覆盖
逐行精读了全部源文件：main.cpp, game.h(294行), game.cpp(~2900行), player.h(93行), player.cpp(298行), tilemap.h/cpp(235行), tile.h/cpp(38行), maploader.h/cpp(175行), skill.h/cpp(665行), enemy.h/cpp(418行), pet.h/cpp(195行), spawner.h/cpp(127行), Qt-Gaming.pro, resources.qrc(150行)。

**文档质量**：✅ 一致性检查通过，无矛盾；所有功能均与当前代码一致。

---

### 【修改 #23】2026-06-04 —— 修复 bomb.gif 爆炸动画完全不播放

#### 修改原因
`createExplosion()` 函数体被意外清空（可能在某次 sed/Python 脚本批量编辑中被删除），只剩 early return guard：
```cpp
void Game::createExplosion(QPointF centerPos)
{
    qDebug() << "[Bomb] createExplosion called, frames=" << g_bombFrames.size();
    if (!scene || g_bombFrames.isEmpty()) return;
    // ← 原本的动画创建代码全部丢失！
}
```
导致 `g_bombFrames` 虽然正确预加载了 144×144 的 bomb.gif 帧，但从未创建 QGraphicsPixmapItem 来显示它们。所有调用点（投射物命中、危险区域爆炸）都只打印了 debug 日志，但屏幕上无任何动画。

#### 修改的文件
| 类型 | 文件 | 说明 |
|------|------|------|
| ✏️ 修改 | `game.cpp` | 补回 `createExplosion()` 完整动画逻辑 |

#### 具体变更

**game.cpp** — `createExplosion()` 从空壳补回 25 行：

新增代码：创建 `QGraphicsPixmapItem` → 设置首帧 → Z=999999 → 144px居中偏移72 → `scene->addItem` → QTimer 8ms 手动切帧 → 播完自动清理。

**技术要点**：
- Z 值 999999 确保炸弹在所有物体之上，仅低于变身动画 (10000000)
- 144px 尺寸居中：`setPos(centerPos.x() - 72, centerPos.y() - 72)`
- QTimer 8ms 间隔 → 约 125 FPS 帧率 → bomb.gif 约 0.38 秒播完
- 内存安全：`delete frameIdx` + `timer->deleteLater()` + 检查 `bombItem->scene()` 防止 double-free

#### 编译验证
```bash
cd /Users/explorer/Desktop/code/Qt-Gaming-main
make clean && qmake && make -j4
```
✅ **编译成功，0 错误，0 警告**

#### 影响范围
- I 技能火球命中敌人时的爆炸 ✅ 恢复
- 西南联大(lianda)地图危险区域倒计时结束时的爆炸 ✅ 恢复
- 所有通过 `createExplosion()` 触发的爆炸效果 ✅ 恢复

---

> **文档结束**。如有新增功能或代码变更，请及时更新此文档，保持与代码同步。
